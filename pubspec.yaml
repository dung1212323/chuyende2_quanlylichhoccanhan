name: course_tracker
description: Ứng dụng quản lý lịch học cá nhân
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.1
  table_calendar: ^3.0.9
  flutter_local_notifications: ^17.0.0
  path_provider: ^2.1.1
  intl: ^0.19.0
  share_plus: ^7.2.1
  cupertino_icons: ^1.0.6
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
